stages:
  - build
  - deploy

include:
  - component: $CI_SERVER_FQDN/infrastructure/latex-build-base/latex-build@1.0.0
    inputs:
      stage: build

deploy:
  stage: deploy
  trigger:
    project: dylan/resume-website
